% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colors.R
\name{ekio_pal}
\alias{ekio_pal}
\title{Get Color Palette}
\source{
The brand scales are generated from one OKLCH specification in
\code{data-raw/build-ramps.R}: a shared lightness spine anchored on the brand
navy, a shared chroma arc, and a hue path per family. The scientific
palettes come from matplotlib (\code{"viridis"}, \code{"inferno"}, \code{"plasma"}) and
from Okabe & Ito (\code{"okabe_ito"}). Notices are in \code{inst/COPYRIGHTS}.
}
\usage{
ekio_pal(palette = "full", n = NULL, reverse = FALSE)
}
\arguments{
\item{palette}{Character. Name of the palette. See \code{\link[=list_ekio_palettes]{list_ekio_palettes()}}
for all available options.}

\item{n}{Integer or NULL. Number of colors to return. If NULL, returns all,
except \code{"accent_blue"} and \code{"accent_orange"}, which return four by
default. For sequential and diverging palettes, \code{n} colors are interpolated
across the full range. For the two variable-size accent palettes, \code{n} can
be between 2 and 6. For \code{"gold"}, other categorical palettes, and
scientific palettes, the first \code{n} colors are taken, interpolating only if
\code{n} exceeds the palette length.}

\item{reverse}{Logical. If TRUE, reverses the palette order.}
}
\value{
Object of class \code{ekio_palette} (a character vector of hex
codes). Printing displays a visual swatch. Use \code{\link[=as.character]{as.character()}} to
strip the class.
}
\description{
Returns colors for data visualization. Includes EKIO brand scales, accent
and categorical palettes, and standard scientific palettes.
When printed interactively, displays the palette as a colored swatch with
hex labels.
}
\details{
Brand scales (\code{"blue"}, \code{"gray"}, \code{"stone"}, \code{"teal"}, \code{"green"},
\code{"orange"}, \code{"red"}) are nine-step ramps running light to dark, named by
shade. Position and shade are aligned by construction, so
\code{ekio_pal("blue")[7]} and \code{ekio_pal("blue")["700"]} are the same color.

\code{"gold"} is an accent rather than a scale: three colors named \code{"light"},
\code{"mid"} and \code{"deep"}, because a nine-step gold ramp turns brown at the
dark end. They sit on the same lightness rungs as scale shades 300, 400
and 500.

\code{"accent_blue"} and \code{"accent_orange"} put one main color before a sequence
of receding grays. They return four colors by default; \code{n} can be set from
2 to 6 to match the number of series while keeping the main color first.
}
\examples{
ekio_pal("full")
ekio_pal("full", n = 4)
ekio_pal("full", reverse = TRUE)
ekio_pal("accent_blue", n = 5)
ekio_pal("okabe_ito")

# Brand scales are named by shade; position i is shade i * 100
ekio_pal("blue")["700"]
ekio_pal("blue")[7]

# gold is an accent, named rather than numbered
ekio_pal("gold")["mid"]
}
