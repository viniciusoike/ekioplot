% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recipes.R
\name{ekio_areaplot}
\alias{ekio_areaplot}
\title{EKIO Area Plot}
\usage{
ekio_areaplot(
  data,
  x,
  y,
  fill = NULL,
  palette = NULL,
  position = "stack",
  alpha = 1,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  ...
)
}
\arguments{
\item{data}{A data frame}

\item{x}{X-axis variable (supports data-masking)}

\item{y}{Y-axis variable (supports data-masking)}

\item{fill}{Fill aesthetic. A color string or a discrete variable. A
continuous variable is an error: bin it or wrap it in \code{factor()}.}

\item{palette}{Character. Palette name for variable mappings.}

\item{position}{Character. Stacking method: \code{"stack"} (default) or
\code{"fill"} for proportional areas.}

\item{alpha}{Numeric. Fill transparency (default: 0.8).}

\item{title, subtitle, caption}{Plot labels. NULL (default) draws none.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:geom_ribbon]{ggplot2::geom_area()}}.
These override the recipe's own geom defaults.}
}
\value{
ggplot2 object
}
\description{
Professional area plot with smart aesthetic detection. Supports stacked
and filled (proportional) area charts.
}
\examples{
\dontshow{.op <- options(ekioplot.font_title = "serif", ekioplot.font_text = "sans")}
ekio_areaplot(ggplot2::economics, date, unemploy)

# Stacked area with groups
data(fuels)
world_fuels <- fuels[fuels$entity == "World" & fuels$year >= 1950, ]
ekio_areaplot(world_fuels, year, consumption_gwh, fill = fuel)
\dontshow{options(.op)}
}
